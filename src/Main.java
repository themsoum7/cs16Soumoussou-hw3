/**
 * Created by Oles Dobosevych on 19-Sep-16.
 */
public class HomeTask {
    /**
     * Finds all elements of array that are divisible by 3 or 7.
     *
     * @param arr an array of integer numbers.
     * @return an array of all integer numbers ordered in the same way that are divisible by 3 or 7.
     */
    public static void main getDivisible(int[] arr) {
    	for(Integer number: division){
    		  if(number % 3 == 0){
    		    System.out.println(number);
    		  }
    		  return arr;
    		}
    HomeTask.getDivisible(new int[]{1, 2, 3})
    }


    /**
     * Finds the index of a first magic number. Magic number is the number in which all digits are in increasing order.
     * If there is no magic number in the array the method should return -1.
     *
     * @param arr an array of integer numbers.
     * @return finds the index of a first magic number. If there is no magic number in the array the method returns -1.
     */
    public static int getMagicNumber(int[] arr) {

    }

    /**
     * Finds all numbers that has the least number of unique digits.
     *
     * @param arr an array of integer numbers.
     * @return  an array of all integer numbers ordered in the same way that has the least number of unique digits.
     */
    public static int[] getWithUniqueDigits(int[] arr) {

    }
}
